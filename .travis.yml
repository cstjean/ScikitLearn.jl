language: julia
os:
    - linux
    - osx
julia:
    - release
    - nightly
notifications:
    email: false
env:
    global:
    - PYTHON=conda
before_install:
    #install scikit-learn to test functionallity
  - if [ $MPMATH = "true" ]; then julia -e 'Pkg.add("Conda"); using Conda; Conda.add("scikit-learn")';fi
#script:
#    - if [[ -a .git/shallow ]]; then git fetch --unshallow; fi
#    - julia --check-bounds=yes -e 'Pkg.clone(pwd()); Pkg.build("ScikitLearn"); Pkg.test("ScikitLearn"; coverage=true)'